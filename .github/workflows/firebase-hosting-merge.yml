name: Deploy Frontend to Firebase Hosting and Backend to Cloud Run

on:
  push:
    branches:
      - main  # Trigger the workflow on push to the main branch

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Install dependencies and build the frontend
      - name: Install dependencies and build frontend
        run: npm ci && npm run build

      # Step 3: Deploy frontend to Firebase Hosting
      - name: Deploy frontend to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}  # GitHub Token for authentication
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DECKHUBAPP }}  # Firebase service account key
          channelId: live  # Deploy to the live channel on Firebase
          projectId: deckhubapp  # Firebase project ID

      # Step 4: Set up Google Cloud SDK for Cloud Run
      - name: Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v1
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}  # Google Cloud project ID
          service_account_key: ${{ secrets.GCP_SA_KEY }}  # Google Cloud service account key

      # Step 5: Authenticate Docker with GCR
      - name: Authenticate Docker with GCR
        run: gcloud auth configure-docker

      # Step 6: Build the Docker image for Cloud Run
      - name: Build the Docker image for Cloud Run
        run: |
          docker build -t gcr.io/${{ secrets.GCP_PROJECT_ID }}/backend-cloud:${{ github.sha }} .

      # Step 7: Push the Docker image to GCR
      - name: Push the Docker image to GCR
        run: |
          docker push gcr.io/${{ secrets.GCP_PROJECT_ID }}/backend-cloud:${{ github.sha }}

      # Step 8: Deploy the backend to Google Cloud Run
      - name: Deploy the backend to Cloud Run
        run: |
          gcloud run deploy deckhub-backend \
            --image gcr.io/${{ secrets.GCP_PROJECT_ID }}/backend-cloud:${{ github.sha }} \
            --platform managed \
            --region us-central1 \
            --allow-unauthenticated \
            --set-env-vars "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}"
